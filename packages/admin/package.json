{
  "name": "@underdog/admin",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@underdog/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  }
}
